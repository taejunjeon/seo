# AIBIO 채널톡 전환 퍼널 검토

작성 시각: 2026-04-22 13:56 KST
기준일: 2026-04-22
대상 사이트: `https://aibio.ai`
관련 프로젝트: AIBIO 리커버리랩스, 바이오컴 Meta CAPI

## 10초 요약
- AIBIO 홈페이지의 채널톡 버튼 클릭과 메신저 열림은 Meta 전환 퍼널의 일부로 잡을 수 있다.
- 이미 `GTM-T8FLZNT` 게시본에는 `aibio_channeltalk_open` 이벤트가 있고, GA4와 Meta Pixel `trackCustom`으로 보내는 구조가 들어 있다.
- 다만 이 이벤트는 `Lead`가 아니라 상담 의도에 가까운 마이크로 전환이다. 광고 최적화의 1차 목표는 계속 `aibio_form_submit -> Lead`로 둔다.
- 다음 행동은 실제 버튼 클릭 1건을 GTM Preview, Meta Pixel Helper, Events Manager Test Events에서 확인하고, 버튼 클릭과 실제 상담 시작을 분리할지 결정하는 것이다.

## Phase-Sprint 요약표
| Phase | Sprint | 이름 | 담당 | 상태(우리/운영) | 상세 |
|---|---|---|---|---|---|
| Phase1 | [[#Phase1-Sprint1]] | 현재 설치 상태 확인 | Codex | 100% / 70% | [[#Phase1-Sprint1\|이동]] |
| Phase1 | [[#Phase1-Sprint2]] | 채널톡 클릭 이벤트 검증 | TJ+Codex | 70% / 40% | [[#Phase1-Sprint2\|이동]] |
| Phase1 | [[#Phase1-Sprint3]] | Lead 승격 기준 확정 | TJ+Codex | 60% / 20% | [[#Phase1-Sprint3\|이동]] |
| Phase2 | [[#Phase2-Sprint4]] | CAPI mirror 판단 | TJ+Codex | 45% / 10% | [[#Phase2-Sprint4\|이동]] |
| Phase2 | [[#Phase2-Sprint5]] | 광고 운영 반영 | TJ+Codex | 35% / 10% | [[#Phase2-Sprint5\|이동]] |

## 문서 목적
이 문서는 AIBIO 홈페이지 채널톡 버튼을 Meta 광고 퍼널에서 어떤 의미로 기록하고, 어디까지 전환 최적화에 써야 하는지 정리한다.

## 이 작업이 하는 일
이 작업은 `광고 클릭 -> 홈페이지 방문 -> 채널톡 열기 -> 실제 상담 시작 -> 방문 예약` 흐름을 분리해서 기록하는 일이다.

채널톡 버튼을 누른 사람은 폼 제출자보다 약한 신호지만, 단순 방문자보다 강한 신호다. 따라서 이 이벤트는 리타겟팅, 광고 소재 비교, 랜딩 개선 판단에 쓸 수 있다.

## 왜 필요한가
AIBIO는 구매 쇼핑몰이 아니라 오프라인 센터 리드 사업이다. 폼 제출만 보면 리드 수는 보이지만, 사용자가 상담을 시도하려는 중간 행동은 놓친다.

채널톡 버튼 클릭을 잡으면 아래 질문에 답할 수 있다.

- 광고 유입자가 페이지를 보고 바로 이탈했는가, 아니면 상담 창까지 열었는가
- 폼 제출은 안 했지만 상담 의도가 있는 사용자가 얼마나 되는가
- 카카오 상담, 채널톡, 폼 제출 중 어떤 CTA가 더 강하게 작동하는가
- 채널톡을 연 사람에게 다시 광고를 보여줄 가치가 있는가

## 결론

### 현재 조건에서 가능한 것
Meta에서 채널톡 버튼 클릭을 전환 퍼널 일부로 잡는 것은 가능하다.

이미 AIBIO GTM 게시본에는 아래 구조가 있다.

| 항목 | 값 |
|---|---|
| GTM 정본 | `GTM-T8FLZNT` |
| Meta Pixel ID | `1068377347547682` |
| 현재 채널톡 이벤트 | `aibio_channeltalk_open` |
| GA4 태그 | `GA4 aibio_channeltalk_open` |
| Meta 태그 | `Meta Custom - aibio_channeltalk_open` |
| Meta 전송 방식 | `fbq('trackCustom')` |
| 이벤트 성격 | 마이크로 전환, 리타겟팅 신호, 퍼널 진단 신호 |

### 아직 하면 안 되는 것
채널톡 버튼 클릭을 바로 Meta `Lead`로 보내면 안 된다.

버튼 클릭은 상담창을 연 행동이다. 상담을 실제로 시작했는지, 연락처를 남겼는지, 방문 예약으로 이어졌는지와 다르다. 이걸 `Lead`로 보내면 Meta가 실제 리드보다 넓은 행동을 리드로 학습할 수 있다.

### 추천 기준
현재는 아래처럼 나눈다.

| 사용자 행동 | Meta 이벤트 | 용도 | 최적화 목표로 사용 |
|---|---|---|---|
| 채널톡 버튼 클릭 또는 메신저 열림 | `aibio_channeltalk_open` custom event | 리타겟팅, 퍼널 진단 | 아직 보류 |
| 채널톡에서 새 상담방 생성 | `aibio_channeltalk_chat_created` custom event | 상담 의도 측정 | 데이터 축적 후 검토 |
| 연락처를 남기거나 상담 신청 완료 | `Lead` standard event | 광고 최적화 | 가능 |
| 방문 예약 확정 | `Schedule` standard event | 고품질 리드 최적화 | 방문 예약 원장 생긴 뒤 가능 |

## 현재 상태

### 확인된 것
- 라이브 HTML에서 Meta Pixel `1068377347547682`가 확인됐다.
- 라이브 HTML에서 GTM `GTM-T8FLZNT`가 확인됐다.
- 라이브 HTML에서 아임웹 채널톡 스크립트 `/js/channel_plugin.js`가 확인됐다.
- GTM 라이브 JS에 `2026-04-17-aibio-micro-events-v1` 마이크로 전환 리스너가 포함되어 있다.
- GTM 라이브 JS에 `aibio_channeltalk_open` GA4 태그와 Meta `trackCustom` 태그가 포함되어 있다.
- Meta Ads Insights API에서 2026-04-17 ~ 2026-04-21 광고 row 5건을 확인했다. 이 기간 광고비는 643,802원이고, action type에는 `aibio_channeltalk_open` 또는 channeltalk 계열 이름이 아직 보이지 않았다.
- 같은 기간 Meta action에는 `lead`, `offsite_conversion.fb_pixel_lead`, `onsite_web_lead`가 각각 10건으로 보였다. `landing_page_view`와 `omni_landing_page_view`는 각각 3,978건으로 같은 값이라 합산하면 안 된다.
- 채널톡 공식 SDK는 `showMessenger`, `onShowMessenger`, `onChatCreated`, `track` 계열 동작을 제공한다.
- 바이오컴 CAPI 문서 기준으로, 브라우저 Pixel과 서버 CAPI를 같이 보낼 때는 같은 `event_id`가 필요하다.

### 아직 안 된 것
- 이번 세션에서는 실제 브라우저에서 버튼을 클릭한 뒤 Meta Events Manager 화면까지 닫지는 못했다.
- 현재 GTM 이벤트는 `채널톡 열림`까지다. 실제 상담방 생성이나 메시지 전송 완료는 아직 별도 이벤트로 분리되어 있지 않다.
- AIBIO 홈페이지에는 같은 Pixel ID로 PageView를 보내는 코드가 두 군데 보인다. 채널톡 이벤트가 중복 발화되는지 확인해야 한다.
- 서버 CAPI 경량 endpoint `/api/meta/capi/track`는 현재 허용 origin에 `https://aibio.ai`가 없다. AIBIO에서 CAPI mirror를 쓰려면 승인 후 CORS 허용 origin을 추가해야 한다.

### 지금 막힌 이유
Meta Events Manager와 ChannelTalk 관리자 화면은 로그인과 권한이 필요한 운영 화면이다. Codex는 공개 HTML, GTM JS, 로컬 코드, 이미 있는 API 경로는 확인할 수 있지만, Meta 화면의 실시간 이벤트 수신 상태와 ChannelTalk 상담방 생성 상태는 TJ님 권한 확인이 필요하다.

### 현재 주체
- Codex: 공개 페이지, GTM 게시본, 로컬 CAPI 코드, 문서 기준 검토
- TJ: Meta Events Manager 화면 확인, ChannelTalk 관리자 권한 확인, 운영 전환 승격 승인
- Claude Code: 필요 시 아임웹/GTM 삽입 코드 미세 수정

## 퍼널 정의

### AIBIO 권장 퍼널
| 순서 | 단계 | 이벤트 | 의미 | 운영 판단 |
|---:|---|---|---|---|
| 1 | 광고 클릭 | Meta Ads click, UTM, fbclid | 광고 유입 시작 | 광고비 분모 |
| 2 | 랜딩 페이지 조회 | `PageView`, `landing_page_view` | 사이트 도착 | 리드 아님 |
| 3 | 관심 행동 | `aibio_engaged_60s`, `aibio_scroll_90` | 콘텐츠 소비 | 약한 마이크로 전환 |
| 4 | 상담 의도 | `aibio_channeltalk_open`, `aibio_kakao_click` | 상담 채널 진입 | 강한 마이크로 전환 |
| 5 | 실제 리드 | `aibio_form_submit -> Lead` | 연락처/상담 신청 | 현재 1차 최적화 후보 |
| 6 | 실제 상담 시작 | `aibio_channeltalk_chat_created` | 채널톡 상담방 생성 | 추가 구현 후보 |
| 7 | 방문 예약 | `Schedule` 또는 내부 `visit_booked` | 방문 일정 확정 | 최종 고품질 전환 후보 |
| 8 | 방문/결제 | 내부 상담/방문 장부 | 센터 매출 연결 | OMTM 계산 |

### 채널톡 버튼의 위치
채널톡 버튼 클릭은 4단계다. 즉 `Lead` 전 단계다.

이 단계는 아래 용도로 유효하다.

- 채널톡을 연 사용자 리타겟팅
- 광고 소재별 상담 의도 비교
- 랜딩 CTA 위치와 문구 비교
- 폼 제출 이전 이탈 구간 분석

반대로 아래 용도로는 아직 부적절하다.

- Meta 리드 캠페인의 최종 최적화 이벤트
- 상담팀 실적 기준
- 방문 예약 수 대체 지표
- 센터 매출 ROAS 계산 지표

## Meta 활용 판단

### 바로 쓸 수 있는 방식
Meta Events Manager에서 `aibio_channeltalk_open` custom event가 들어오는지 확인한 뒤, 아래처럼 쓴다.

| 쓰임 | 판단 |
|---|---|
| Ads Manager 보조 컬럼 | 가능 |
| Custom Conversion | 가능 |
| Website Custom Audience | 가능 |
| 리타겟팅 캠페인 audience | 가능 |
| 리드 최적화 이벤트 | 아직 보류 |
| 방문 예약 최적화 이벤트 | 아직 보류 |

현재 Meta Ads Insights API에는 2026-04-17 ~ 2026-04-21 기준 `aibio_channeltalk_open` action type이 보이지 않았다. 따라서 "GTM에 설치되어 있다"와 "광고관리자에서 전환 컬럼으로 이미 잡힌다"는 아직 같은 말이 아니다. Custom Conversion 생성과 Events Manager 수신 확인이 먼저다.

### Custom Conversion 추천 설정
| 항목 | 추천값 |
|---|---|
| 이름 | `AIBIO - ChannelTalk Open` |
| 데이터 소스 | Pixel `1068377347547682` |
| 이벤트 | `aibio_channeltalk_open` |
| URL 조건 | `aibio.ai` 포함 |
| 값 | 없음 |
| 사용 목적 | Ads Manager 컬럼, 리타겟팅 audience, 랜딩/CTA 진단 |

### 광고 최적화에 쓰는 조건
`aibio_channeltalk_open`을 광고세트 최적화 이벤트로 쓰려면 아래 조건이 먼저 필요하다.

1. 같은 클릭 1회에서 이벤트가 1회만 발생한다.
2. 자동 팝업이나 재방문 자동 열림이 전환으로 섞이지 않는다.
3. 최근 7일 기준 이벤트 수가 충분하다.
4. `aibio_channeltalk_open -> form_submit -> 상담/방문` 전환율이 확인된다.
5. Meta Events Manager에서 이벤트 품질 경고가 없다.

이 조건이 닫히기 전에는 `Lead` 또는 내부 `form_submit`을 최적화 중심으로 유지한다.

## CAPI 관점

### 바이오컴 CAPI 문서에서 가져올 점
`meta/capimeta.md`의 핵심은 단순하다.

브라우저 Pixel만 쓰면 브라우저 차단, 쿠키 제한, 네트워크 실패로 일부 이벤트가 빠질 수 있다. 서버 CAPI를 같이 쓰면 보완할 수 있다. 단, 브라우저와 서버가 같은 행동을 동시에 보낼 때는 `event_id`와 이벤트명이 같아야 Meta가 중복을 줄인다.

이 원칙은 AIBIO에도 적용된다. 다만 채널톡 버튼 클릭은 아직 최종 전환이 아니므로, 처음부터 서버 CAPI까지 붙일 필요는 낮다.

### 현재 서버 코드 상태
로컬 코드 기준으로 `/api/meta/capi/track` 경량 endpoint는 이미 있다.

| 항목 | 현재 상태 |
|---|---|
| endpoint | `POST /api/meta/capi/track` |
| 허용 이벤트 | `ViewContent`, `AddToCart`, `InitiateCheckout`, `Lead`, `Search` |
| AIBIO Pixel 허용 | env 기본값에 `1068377347547682` 존재 |
| CORS 허용 origin | `biocom.kr`, `thecleancoffee.com`, `localhost:7010` 중심 |
| AIBIO origin | 현재 미포함 |

따라서 AIBIO에서 CAPI mirror를 쓰려면 아래 변경이 필요하다.

1. 승인 후 `FUNNEL_ALLOWED_ORIGINS`에 `https://aibio.ai`, `https://www.aibio.ai`, `https://aibio.imweb.me`를 추가한다.
2. 버튼 클릭을 CAPI로 보낼지, 실제 상담방 생성만 CAPI로 보낼지 결정한다.
3. 브라우저 Pixel과 CAPI가 같은 `event_id`를 쓰게 만든다.
4. Meta Test Events에서 browser/server dedup을 확인한다.

### CAPI를 붙이는 우선순위
| 이벤트 | CAPI 필요성 | 이유 |
|---|---|---|
| `aibio_channeltalk_open` | 낮음 | 버튼 열림은 마이크로 신호라 browser-only로 먼저 충분하다. |
| `aibio_channeltalk_chat_created` | 중간 | 실제 상담 시작이면 누락 보완 가치가 있다. |
| `Lead` | 높음 | 광고 최적화 핵심 이벤트다. 중복 방지와 품질 확인이 중요하다. |
| `Schedule` | 높음 | 방문 예약은 고품질 전환이다. 서버 원장 기준으로 보내야 한다. |

## 구현 판단

### 현재 GTM 구조
현재 GTM listener는 두 경로로 `aibio_channeltalk_open`을 만든다.

| 감지 경로 | 의미 |
|---|---|
| `ChannelIO('onShowMessenger')` | 채널톡 메신저가 실제로 열림 |
| `#ch-plugin`, `#ch-plugin-entry`, `[id^="ch-plugin"]` 클릭 | 채널톡 위젯 클릭 |

그리고 한 페이지에서 같은 이벤트가 여러 번 나가지 않도록 `window.__AIBIO_GTM_MICRO_aibio_channeltalk_open` 플래그로 중복을 막는다.

### 이 구조의 장점
- 실제 버튼이 어디에 렌더링되든 `id^="ch-plugin"` 계열로 잡을 수 있다.
- ChannelTalk SDK가 늦게 로드되어도 30회까지 바인딩을 재시도한다.
- GA4와 Meta가 같은 `dataLayer` 이벤트를 기준으로 움직인다.
- Meta에는 custom event로 보내므로 `Lead` 전환을 오염시키지 않는다.

### 보완할 점
현재 이벤트 이름은 `open`이다. 순수하게 버튼 클릭만 보고 싶으면 아래처럼 분리하는 것이 더 정확하다.

| 이벤트 | 발화 조건 | 쓰임 |
|---|---|---|
| `aibio_channeltalk_button_click` | 채널톡 위젯 직접 클릭 | 버튼 CTA 성과 |
| `aibio_channeltalk_open` | 메신저 실제 열림 | 상담 채널 진입 |
| `aibio_channeltalk_chat_created` | 새 상담방 생성 | 실제 상담 시작 |

현재는 `button_click`과 `open`을 하나로 묶고 있다. 운영 초기에는 괜찮지만, 광고 최적화에 쓰려면 분리하는 편이 낫다.

## 검증 절차

### 1. 브라우저 검증
1. Chrome 시크릿 창에서 `https://aibio.ai`를 연다.
2. Meta Pixel Helper를 켠다.
3. 채널톡 버튼을 1회 클릭한다.
4. `aibio_channeltalk_open` custom event가 1회만 보이는지 확인한다.
5. 이 클릭만으로 `Lead`가 뜨지 않는지 확인한다.

### 2. 콘솔 검증
채널톡 버튼 클릭 후 콘솔에서 아래 값을 확인한다.

```js
window.__AIBIO_META_MICRO_LAST__ &&
window.__AIBIO_META_MICRO_LAST__.aibio_channeltalk_open
```

기대값은 `eventId`, `sentAt`, `payload.interaction_type = channeltalk_open`이 있는 객체다.

### 3. GTM Preview 검증
1. GTM Preview에서 AIBIO URL을 연결한다.
2. 채널톡 버튼을 클릭한다.
3. 이벤트 타임라인에 `aibio_channeltalk_open`이 뜨는지 확인한다.
4. `GA4 aibio_channeltalk_open` 태그가 1회 실행되는지 확인한다.
5. `Meta Custom - aibio_channeltalk_open` 태그가 1회 실행되는지 확인한다.

### 4. Meta Events Manager 검증
1. Pixel `1068377347547682`의 Test Events를 연다.
2. 테스트 URL에 `https://aibio.ai`를 입력한다.
3. 채널톡 버튼을 누른다.
4. `aibio_channeltalk_open`이 custom event로 들어오는지 확인한다.
5. 이벤트 세부값에 event ID가 있는지 확인한다.
6. 이벤트가 2회 이상 들어오면 GTM listener와 Pixel 중복을 먼저 점검한다.

### 5. Ads Manager 검증
1. Custom Conversion `AIBIO - ChannelTalk Open`을 만든다.
2. 광고관리자 컬럼에 해당 custom conversion을 추가한다.
3. 최근 7일 기준 광고별 `채널톡 열림 / 링크 클릭 / 랜딩 페이지 조회 / Lead`를 비교한다.
4. `채널톡 열림은 많지만 Lead가 적은 광고`와 `채널톡 열림은 적지만 Lead가 높은 광고`를 분리한다.

## 위험과 주의

### 위험 1. 버튼 클릭을 Lead로 오해
채널톡 버튼 클릭은 연락처 제출이 아니다. 이 이벤트를 `Lead`로 보내면 Meta 리드 수가 부풀 수 있다.

대응: `aibio_channeltalk_open`은 custom event로 유지한다. 실제 폼 제출 또는 상담 신청 완료만 `Lead`로 보낸다.

### 위험 2. 자동 열림과 직접 클릭 혼합
`onShowMessenger`는 사용자가 직접 클릭했을 때뿐 아니라 코드나 자동 동작으로 메신저가 열릴 때도 실행될 수 있다.

대응: 순수 버튼 클릭이 필요하면 `aibio_channeltalk_button_click`을 별도 이벤트로 분리한다.

### 위험 3. Pixel 중복 설치
라이브 HTML에는 같은 Pixel ID `1068377347547682`로 PageView를 보내는 코드가 두 구간에서 보인다.

대응: 우선 PageView 중복과 채널톡 custom event 중복을 Pixel Helper에서 확인한다. 중복이 있으면 수동 Pixel 코드와 아임웹 내장 Pixel 중 하나를 정리한다.

### 위험 4. 민감 정보 전송
AIBIO는 건강, 회복, 컨디션 개선 맥락의 서비스다. Meta로 증상, 질환, 상담 메시지 본문, 전화번호, 이메일 원문을 보내면 안 된다.

대응: Meta에는 이벤트명, URL, generic parameter만 보낸다. 고객 식별자는 필요할 때만 해시 또는 Meta가 허용하는 형식으로 처리한다. 상담 내용은 내부 장부에만 둔다.

### 위험 5. CAPI 중복
나중에 CAPI mirror를 켜면 browser event와 server event가 둘 다 Meta에 도착한다.

대응: 같은 행동은 같은 `event_id`와 같은 이벤트명으로 보낸다. Meta 공식 문서 기준으로 Pixel의 `eventID`와 CAPI의 `event_id`가 맞아야 중복 제거가 된다.

## 역할 구분

### TJ가 할 일
- Meta Events Manager에서 Pixel `1068377347547682`의 Test Events 화면을 연다.
- 채널톡 버튼 클릭 후 `aibio_channeltalk_open`이 1회 들어오는지 확인한다.
- 이 이벤트를 Custom Conversion으로 만들지 승인한다.
- ChannelTalk 관리자에서 새 상담방 생성 이벤트 또는 webhook/API 접근 가능 여부를 확인한다.
- 채널톡 클릭을 광고 최적화 목표로 쓸지, 리타겟팅용으로만 둘지 결정한다.

### Codex가 할 일
- GTM live JS와 AIBIO HTML 기준으로 이벤트 설치 상태를 점검한다.
- `aibio_channeltalk_open` 중복 발화 여부를 기록한다.
- CAPI mirror가 필요해지면 AIBIO origin 허용, event_id 규칙, Test Events 검증 계획을 만든다.
- 내부 `form_submit` 원장과 채널톡 open 이벤트를 같은 광고/UTM 기준으로 비교할 분석표를 만든다.

### Claude Code가 할 일
- 필요 시 GTM/아임웹 삽입 코드에서 `button_click`, `open`, `chat_created` 이벤트를 분리한다.
- 운영자가 읽을 수 있는 광고별 퍼널 표를 만든다.
- 홈페이지 CTA 문구와 버튼 위치 실험을 정리한다.

## Phase별 계획

### Phase 1
[[#Phase-Sprint 요약표|요약표로]]

**이름**: 채널톡 버튼 이벤트를 안정적으로 수집한다.

#### Phase1-Sprint1
[[#Phase-Sprint 요약표|요약표로]]

**이름**: 현재 설치 상태 확인
**상태**: 100% / 70%

- 무엇을 하는가: AIBIO 라이브 HTML, GTM JS, 로컬 CAPI 코드에서 채널톡 이벤트가 들어갈 기반이 있는지 확인한다.
- 왜 필요한가: 이미 있는 이벤트를 모르고 새로 만들면 중복 이벤트가 생긴다.
- 산출물: 이 문서의 현재 상태, 구현 판단, 위험 목록
- 우리 프로젝트에 주는 도움: 채널톡 버튼을 새 전환으로 만들기 전에 중복과 과대계상을 막는다.

##### 역할 구분
- TJ: 해당 없음
- Codex: 라이브 HTML, GTM JS, 로컬 CAPI 코드 확인
- Claude Code: 해당 없음

##### 실행 단계
1. ~~[Codex] 라이브 HTML에서 Pixel `1068377347547682` 확인.~~
2. ~~[Codex] 라이브 HTML에서 GTM `GTM-T8FLZNT` 확인.~~
3. ~~[Codex] 라이브 HTML에서 `/js/channel_plugin.js` 확인.~~
4. ~~[Codex] GTM JS에서 `aibio_channeltalk_open` 태그 확인.~~
5. ~~[Codex] `/api/meta/capi/track`의 AIBIO origin 허용 여부 확인.~~

#### Phase1-Sprint2
[[#Phase-Sprint 요약표|요약표로]]

**이름**: 채널톡 클릭 이벤트 검증
**상태**: 70% / 40%

- 무엇을 하는가: 실제 버튼 클릭 1건이 GA4와 Meta에 각각 1회만 들어오는지 확인한다.
- 왜 필요한가: 전환 이벤트는 1회 행동이 1회 기록될 때만 광고 판단에 쓸 수 있다.
- 산출물: Pixel Helper 캡처, GTM Preview 캡처, Events Manager Test Events 캡처
- 우리 프로젝트에 주는 도움: 마이크로 전환을 광고 리타겟팅과 소재 판단에 쓸 수 있게 된다.

##### 역할 구분
- TJ: Meta Events Manager Test Events 화면 확인
- Codex: 검증 절차 정리, 중복 의심 원인 분석
- Claude Code: 필요 시 GTM listener 수정

##### 실행 단계
1. [TJ] 시크릿 창에서 `https://aibio.ai` 접속 후 채널톡 버튼 1회 클릭.
2. [TJ] Meta Pixel Helper에서 `aibio_channeltalk_open` 1회 수신 확인.
3. [TJ] 이 클릭만으로 `Lead`가 뜨지 않는지 확인.
4. [TJ] Events Manager Test Events에서 `aibio_channeltalk_open` 수신 확인.
5. [Codex] 결과 캡처 기준으로 이벤트 중복 여부 판단.
6. [Claude Code] 중복이면 `button_click`과 `open` 분리 또는 Pixel 중복 정리안 작성.

#### Phase1-Sprint3
[[#Phase-Sprint 요약표|요약표로]]

**이름**: Lead 승격 기준 확정
**상태**: 60% / 20%

- 무엇을 하는가: 채널톡 관련 행동 중 무엇을 `Lead`, `Contact`, `Schedule`로 볼지 정한다.
- 왜 필요한가: 약한 행동을 강한 전환으로 보내면 Meta가 잘못 학습한다.
- 산출물: 전환 사전, 이벤트명, 승격 기준표
- 우리 프로젝트에 주는 도움: 광고 최적화가 실제 상담과 방문 예약에 가까워진다.

##### 역할 구분
- TJ: 어떤 채널톡 상태를 실제 상담 리드로 볼지 승인
- Codex: 이벤트명과 Meta 전송 기준 정리
- Claude Code: 채널톡 SDK 이벤트 분리 구현 보조

##### 실행 단계
1. [TJ] 채널톡에서 `새 상담방 생성`, `연락처 수집`, `방문 예약 확정`을 구분할 수 있는지 확인.
2. [Codex] `aibio_channeltalk_open`, `aibio_channeltalk_chat_created`, `Lead`, `Schedule` 기준표 확정.
3. [TJ+Codex] `aibio_channeltalk_open`은 custom event로 유지하는지 승인.
4. [TJ+Codex] 실제 상담 시작부터 `Contact`로 볼지, 연락처 수집부터 `Lead`로 볼지 결정.

### Phase 2
[[#Phase-Sprint 요약표|요약표로]]

**이름**: 채널톡 이벤트를 광고 운영과 서버 기록에 연결한다.

#### Phase2-Sprint4
[[#Phase-Sprint 요약표|요약표로]]

**이름**: CAPI mirror 판단
**상태**: 45% / 10%

- 무엇을 하는가: 채널톡 이벤트를 브라우저 Pixel만 쓸지, 서버 CAPI까지 병행할지 결정한다.
- 왜 필요한가: 서버 CAPI는 누락 보완에 좋지만, 잘못 붙이면 중복 전환이 된다.
- 산출물: CAPI 적용 여부, AIBIO origin 허용안, event_id 규칙
- 우리 프로젝트에 주는 도움: 중요한 전환이 브라우저 차단으로 빠지는 위험을 줄인다.

##### 역할 구분
- TJ: 보안/배포 변경 승인, AIBIO token 권한 확인
- Codex: CAPI route 수정안, event_id/dedup 설계, Test Events 검증
- Claude Code: 브라우저 snippet 보조

##### 실행 단계
1. [Codex] 현재 `/api/meta/capi/track`는 AIBIO origin 미허용 상태라고 기록.
2. [TJ] AIBIO에서 CAPI mirror가 필요한 이벤트를 결정.
3. [TJ] `https://aibio.ai` origin 허용 변경 승인.
4. [Codex] 승인 후 `FUNNEL_ALLOWED_ORIGINS`에 AIBIO 도메인 추가.
5. [Codex] browser Pixel과 server CAPI가 같은 `event_id`를 쓰는 테스트 진행.

#### Phase2-Sprint5
[[#Phase-Sprint 요약표|요약표로]]

**이름**: 광고 운영 반영
**상태**: 35% / 10%

- 무엇을 하는가: 채널톡 open 이벤트를 광고관리자 보조 지표와 리타겟팅 audience로 쓴다.
- 왜 필요한가: 폼 제출 이전의 상담 의도자를 다시 잡아야 광고비 효율이 오른다.
- 산출물: Custom Conversion, Ads Manager 컬럼, 리타겟팅 audience
- 우리 프로젝트에 주는 도움: 폼 제출만 기다리지 않고 상담 의도층을 빠르게 재공략할 수 있다.

##### 역할 구분
- TJ: Meta custom conversion 생성 승인, 리타겟팅 캠페인 운영 승인
- Codex: 광고별 채널톡 open, Lead, 폼 제출 비교표 작성
- Claude Code: 대시보드/문서 시각화

##### 실행 단계
1. [TJ] Custom Conversion `AIBIO - ChannelTalk Open` 생성.
2. [TJ] Website Custom Audience `AIBIO - ChannelTalk Open 30d` 생성.
3. [Codex] 광고별 `링크 클릭 -> 채널톡 open -> Lead` 표 작성.
4. [TJ+Codex] 채널톡 open 대비 Lead 전환율이 낮은 광고의 랜딩/CTA 개선안 작성.
5. [TJ] 리타겟팅 캠페인 예산 테스트 여부 결정.

## 산출물
- 채널톡 퍼널 판단 문서: `aibio/채널톡퍼널.md`
- 현재 GTM 이벤트 기준: `aibio_channeltalk_open`
- 추천 Custom Conversion 이름: `AIBIO - ChannelTalk Open`
- 추천 audience 이름: `AIBIO - ChannelTalk Open 30d`
- 후속 후보 이벤트: `aibio_channeltalk_button_click`, `aibio_channeltalk_chat_created`, `Schedule`

## 다음 액션
1. TJ님이 Meta Events Manager Test Events에서 채널톡 버튼 클릭 1건을 확인한다.
2. 확인 결과 `aibio_channeltalk_open`이 1회만 들어오면 Custom Conversion을 만든다.
3. 이벤트가 2회 이상 들어오면 Pixel 중복 또는 GTM listener 중복을 먼저 정리한다.
4. 채널톡 실제 상담방 생성 이벤트를 가져올 수 있으면 `aibio_channeltalk_chat_created`를 별도 설계한다.
5. 방문 예약 장부가 생기면 `Schedule` 또는 내부 `visit_booked`를 고품질 전환으로 올린다.

## 참고 문서
- 로컬 AIBIO GTM 기록: `aibio/gtm.md`
- 로컬 AIBIO 로드맵: `aibio/!aibioroadmap.md`
- 로컬 바이오컴 CAPI 가이드: `meta/capimeta.md`
- ChannelIO Web SDK: [https://developers.channel.io/en/articles/0b119290](https://developers.channel.io/en/articles/0b119290)
- Meta CAPI dedup 공식 문서: [https://developers.facebook.com/docs/marketing-api/conversions-api/deduplicate-pixel-and-server-events](https://developers.facebook.com/docs/marketing-api/conversions-api/deduplicate-pixel-and-server-events)

## 개발 부록

### 라이브 페이지 확인값
2026-04-22 13:56 KST에 공개 HTML과 GTM JS를 읽어 확인했다.

| 확인 항목 | 결과 |
|---|---|
| `https://aibio.ai` Meta Pixel | `1068377347547682` 확인 |
| `https://aibio.ai` GTM | `GTM-T8FLZNT` 확인 |
| 채널톡 스크립트 | `/js/channel_plugin.js?1743386991` 확인 |
| GTM 마이크로 이벤트 버전 | `2026-04-17-aibio-micro-events-v1` 확인 |
| GTM 채널톡 이벤트 | `aibio_channeltalk_open` 확인 |
| Meta 전송 함수 | `fbq("trackCustom", "aibio_channeltalk_open", ...)` 확인 |

### Meta API 확인값
2026-04-22 13:58 KST에 기존 Meta 토큰으로 AIBIO 광고 계정 `act_377604674894011`의 최근 인사이트를 읽었다.

| 항목 | 값 |
|---|---:|
| 조회 기간 | 2026-04-17 ~ 2026-04-21 |
| 광고 row | 5 |
| 광고비 | 643,802원 |
| 링크 클릭 | 4,464 |
| 랜딩 페이지 조회 | 3,978 |
| `lead` | 10 |
| `offsite_conversion.fb_pixel_lead` | 10 |
| `onsite_web_lead` | 10 |
| `offsite_content_view_add_meta_leads` | 411 |
| channeltalk/aibio custom action type | 0 |

해석: 채널톡 이벤트는 GTM/Pixel 코드에는 있으나, 광고 인사이트 action type으로는 아직 확인되지 않았다. Events Manager에서 custom event 수신을 먼저 확인하고 Custom Conversion을 만들어야 광고관리자 컬럼에서 안정적으로 볼 수 있다.

### 현재 GTM 이벤트 흐름
```text
ChannelTalk button click 또는 ChannelIO onShowMessenger
  -> dataLayer.push({ event: "aibio_channeltalk_open", ... })
  -> GA4 aibio_channeltalk_open
  -> Meta trackCustom aibio_channeltalk_open
```

### 현재 Meta Lead 흐름
```text
AIBIO form submit
  -> dataLayer.push({ event: "aibio_form_submit", ... })
  -> GA4 generate_lead
  -> Meta Lead
```

### CAPI 확장 후보 흐름
```text
ChannelTalk chat_created 또는 Lead 확정
  -> browser Pixel event with eventID
  -> POST /api/meta/capi/track with same event_id
  -> Meta dedup by event_name + event_id
```

### 금지 payload
Meta로 아래 값은 보내지 않는다.

| 값 | 이유 |
|---|---|
| 상담 메시지 본문 | 민감 정보 가능성 |
| 증상, 질환, 건강 고민 | 민감 정보 가능성 |
| 전화번호 원문 | 개인정보 |
| 이메일 원문 | 개인정보 |
| 상담사가 남긴 내부 메모 | 운영 내부 정보 |

### 검증 기준
- wiki 링크 검증: `python3 scripts/validate_wiki_links.py aibio/채널톡퍼널.md`
- 이벤트 검증: 한 번의 채널톡 클릭에 `aibio_channeltalk_open` 1회
- 전환 검증: 채널톡 클릭만으로 `Lead`가 발화하지 않음
- CAPI 검증: server mirror를 켤 경우 browser/server event_id 일치
