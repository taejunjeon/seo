대상 URL: https://biocom.kr (mobile / desktop 각각)
목표: LCP 36s → 4s 이내, FCP 20s → 2s 이내

1) 이번 측정에서 “LCP 요소”가 정확히 무엇인지(요소 타입: 이미지/영상/텍스트 + 해당 리소스 URL) 알려줘.
2) LCP를 TTFB / 로딩 지연 / 다운로드 시간 / 렌더 지연으로 쪼개서(각 ms) 가장 큰 구간이 어디인지 알려줘.
3) 초기 렌더(FCP)를 막는 상위 10개 리소스(CSS/JS)와 각각의 낭비 시간(ms)을 표로 정리해줘.
4) 네트워크 요청 중 전송량 Top 20을 타입별(이미지/폰트/스크립트/영상)로 묶고,
   - “첫 화면에 꼭 필요한 것” vs “나중에 불러도 되는 것”을 분리해서 추천해줘.
5) 서드파티 도메인별 비용(요청 수/전송량/차단 시간)이 있으면 도메인별로 알려줘.
6) (임웹 기반 제약 고려) 코드 크게 못 바꾼다는 가정에서,
   - 당장 가능한 개선안(이미지 포맷/크기, 영상 로딩 방식, 추적 스크립트 지연, 폰트 정리)을 우선순위로 제안해줘.
