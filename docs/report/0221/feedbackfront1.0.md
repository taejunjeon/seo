프론트 계획(report0221-front1.0) 평가 ✅⚠️
A. 전체 방향: “우선순위(F1 → F2 → F3)”는 맞습니다 ✅

**F1(0점 원인 표시 + 프로바이더별 상세)**만 해도 “의사결정 도구”로 급상승해요.

**F2(PageSpeed 자동 연동)**은 GEO 기술 성능 0/10 같은 ‘빈 점수’를 제거해서 신뢰도를 올립니다.

**F3(감점 요인 고도화/내보내기/히스토리)**는 제품의 “반복 사용성”을 키웁니다.

B. 프론트 설계에서 꼭 고쳐야 할 5가지(중요) 🔧

verdict를 “AEO breakdown detail 텍스트 파싱”으로 만들지 마세요

계획서에 “detail 문구 추출”이 있는데, 이건 유지보수 지옥입니다.
✅ 프론트는 /api/ai/citation의 구조화된 verdict 필드를 그대로 써야 합니다.

텍스트는 번역/문구 변경 순간 깨져요.

AI 인용도는 ‘URL 진단’과 결이 다릅니다 → UX에서 구분 표기 필요

페이지 진단 탭에서 URL을 넣지만, AI 인용도는 기본적으로 “도메인/사이트 단위”에 가까운 지표예요.
✅ UI에 이렇게 써주세요:

“AI 인용도는 이 URL이 아니라 biocom.kr 전체가 AI 답변 출처에 등장하는지 측정합니다.”

그리고 matchedReferences 안에 현재 URL이 있으면 “이 페이지가 인용됨” 배지로 강하게 강조
→ 이러면 ‘페이지 진단’과도 연결이 생깁니다.

SerpAPI 쿼터/대기시간 때문에 ‘진단 모드’가 필요합니다
지금 설계대로면:

PageSpeed 10–30초

AI 인용도(3 프로바이더) 10–30초

합치면 사용자가 “느리다”고 느끼기 쉽습니다.

✅ 권장 UI 옵션

“빠른 진단”: Crawl + AEO/GEO(캐시)만

“정밀 진단”: PageSpeed + AI 인용도 포함(비용/시간 안내)

표본 키워드(pickedQueries) 노출은 좋지만, ‘왜 이 키워드냐’가 필요

실무자는 “왜 하필 이 5개?”를 물어요.
✅ 표본 옆에 한 줄로:

“최근 30일 노출 상위 기준” 또는 “정보성(Q&A) 우선 기준” 등 표시

page.tsx 3000줄은 “기능 추가 속도”를 떨어뜨립니다

계획서의 “diff 줄이기 위해 유지”는 이해하지만, F1은 독립 컴포넌트로 빼도 diff가 작게 나옵니다.
✅ 최소한 이 정도는 분리 추천:

<AiCitationSection />

<ProviderCards />

<CitationSamplesTable />

<CompetitorDomainsTable />