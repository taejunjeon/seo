# 다음할일 0416

기준: 2026-04-16 KST → 2026-04-17 업데이트 반영
판단 데이터: VM `https://att.ainativeos.net`, 바이오컴, 1d click, 2026-04-13~15 닫힌 3일

---

## 2026-04-17 신규 — 광고 캠페인 LTV & 데이터 인프라

### 완료된 것

1. **VM 배포: CAPI 퍼널 필드** — `meta.ts`에 view_content/add_to_cart/initiate_checkout 추출 추가. VM 메모리 부족으로 로컬 빌드 → SCP → PM2 restart 방식 확립.
2. **퍼널 분석 UI** — /ads 페이지에 캠페인별 퍼널 바, A/B/C/D 점수, 병목 자동감지, 기간 선택 버튼 구현.
3. **추정 LTV ROAS** — callprice 검사유형별 상담 전환율·추가매출 데이터로 Meta ROAS에 LTV 승수 적용. 캠페인별 표시.
4. **알러지/음식물 통합** — 동일 검사(라벨만 다름, 2024→2025 전환). callprice 인사이트 블록에 원인 분석 기재. 합산 기준 LTV 승수 적용.
5. **종합→유기산 합산** — 소표본(3건) 종합을 유기산에 합치고 "유기산(종합 포함)" 표시.
6. **영양제 첫구매 LTV API** — `/api/callprice/supplement-first-ltv` 구현. 검사권 vs 영양제 첫구매 고객 비교, 연도별 첫구매 상품 Top3, 6개월 LTR, 검사 전환율(6.7%).
7. **건강기능식품 캠페인 추정 LTV ROAS** — supplement-first 180일 LTR 기반 재구매 추가매출 적용. 광고 유입 보정(재구매율 50% 할인) 반영.
8. **Attribution ledger 점검** — VM `backend-logs/checkout-attribution-ledger.jsonl`에 331건 존재. Meta 유입 48건, 건강기능식품 UTM(`meta_biocom_kimteamjang_supplements`) 13건, YouTube UTM 8건 확인.

### 진행 중

1. **Meta 유입 건강기능식품 코호트 추적** — ledger의 `meta_biocom_kimteamjang_supplements` 13건 + member_code 매칭 → 30/90/180일 재구매율 측정. 표본 적어 1~2주 추가 수집 필요.
2. **CAPI 카드 자동 갱신** — 1시간 간격 자동 refresh 구현됨. POST 기간 시각 명시.
3. **영양중금속검사 캠페인 관찰** — 7d_click ROAS 0.89x, LTV 보정 1.38x. 2주 모니터링 후 판단. UTM 미설정 → Meta 광고 관리자에서 URL 수정 필요.

### 다음 할 일 (우선순위)

| 우선순위 | 항목 | 상세 |
|---------|------|------|
| P1 | Meta 유입 코호트 재구매율 측정 | ledger 13건 → member_code 매칭 → 코호트 LTV 산출. 표본 50건+ 모이면 광고 유입 전용 LTV multiplier 확정 |
| P1 | 영양중금속·호르몬 캠페인 UTM 추가 | Meta 광고 관리자에서 활성 광고 URL에 utm_campaign 파라미터 설정 |
| P2 | YouTube UTM 체계화 | 영상 설명란·고정댓글에 `?utm_source=youtube&utm_campaign=영상제목` 통일. 현재 8건 수집 중이지만 체계적 태깅 필요 |
| P2 | 아임웹 openapi OAuth 확보 | v2 API에 유입분석(referrer) 데이터 없음. openapi.imweb.me 권한 확보하면 referrer 접근 가능할 수도 있음 |
| P3 | 건강기능식품 캠페인 증액 판단 | 광고 유입 전용 LTV ROAS ≥ 2.0x → 증액 가능, 1.5~2.0x → 보수적 증액(10~20%), < 1.5x → 크리에이티브 최적화 선행 |

### 핵심 인사이트 (의사결정 참고)

- **영양제 첫구매 고객 재구매율 45%, 검사권 17%** — 그러나 광고 유입 고객은 30~50% 낮을 것으로 추정 (업계 통상). 초기 실측: Meta 유입 건강기능식품 14명 중 재구매 3명(21.4%) → 50% 보정과 일치
- **재구매의 65%가 90일 이내** — 정기구독 중심. 30일 내 28% 재구매
- **supplement-first → test-kit 전환 6.7%** — 영양제 캠페인을 검사 퍼널 입구로 기대하기 어려움
- **2025년 당당케어(혈당관리) 급부상** — 영양제 첫구매 1위 (911건), 타겟 광고 확대 검토
- **7d_click+1d_view vs 7d_click 차이** — 뷰스루 어트리뷰션에 다른 상품 구매 포함됨. 예산 판단은 7d_click 기준
- **GA4로 유입 채널별 결제 추적 이미 가능** — 아임웹 API에 유입분석 없어도 GA4 API(`/api/ga4/source-conversion`)로 source/medium별 구매 전환 조회 가능
- **YouTube 유입 전환율이 Meta보다 높음** — YouTube 당당케어 5.6%(36건/644세션) vs Meta kimteamjang 0.9%(31건/3,372세션). 유튜브 UTM 체계화 시 정확한 ROI 측정 가능

---

## 2026-04-17 업데이트 — 지금 최우선

현재 최우선은 **Conversion Dictionary v1 승인 패키지와 freshness/source-of-truth 고정**이다.

기존 0416 문서의 캠페인 매핑/LTV ROAS 작업은 중요하지만, 지금은 `정본 전환`과 `freshness`가 먼저다. 이유는 캠페인 ROAS, CAPI, Revenue Integrity Agent, iROAS가 모두 같은 전환 정의를 공유해야 하기 때문이다. 정의가 흔들리면 대시보드와 에이전트가 서로 다른 숫자를 설명하게 된다.

실행 비중은 아래로 둔다.

| 구분 | 비중 | 지금 할 일 |
|---|---:|---|
| 정본/freshness 작업 | 80% | Conversion Dictionary v1 승인, 사이트별 source-of-truth, CAPI/Purchase Guard/sync 검증 |
| 얇은 loop 운영 방식 | 20% | 복잡한 작업에만 `plans/*.md`, evidence-first 보고, 재검증 task 남기기 |
| 보류 | 0% | `.codex/agents`, `.claude/agents`, 자동 PR, recursive fan-out, production write-back 자동화 |

### Conversion Dictionary v1 진행 상태

`Conversion Dictionary v1`은 **초안 완료, 운영 승인 대기** 상태다.

완료된 것:

- `roadmap/phase0_codex_draft.md`에 `paid`, `confirmed`, `pending`, `canceled`, `refunded`, `VirtualAccountIssued` 정의 초안 작성.
- Imweb/Toss/Attribution Ledger/Meta CAPI log/GA4/Browser Pixel별 상태 매핑표 작성.
- GA4, Meta, CAPI log, Imweb, Toss, Attribution Ledger, CRM DB의 freshness 판정 규칙 작성.
- `/api/integrity/health`, `/dictionary`, `/source-of-truth`, `/summary` 계열 backend contract 초안 작성.
- `roadmap/phase0.md`에 "설계 초안 완료 -> TJ님 승인 대기"로 정리.

아직 안 된 것:

- TJ님이 운영 `confirmed` 기준을 승인하지 않았다.
- `paid`를 ROAS 분자에서 제외할지, `paidRevenue/provisionalRevenue`로만 볼지 승인 전이다.
- 환불/부분환불, 배송비/VAT 포함 여부, CAPI Purchase 전송 기준이 승인 전이다.
- 승인된 정의가 아직 `/api/integrity/*`, `/ads`, `/tracking-integrity`의 운영 계약으로 반영되지 않았다.

따라서 다음 작업은 새 기능 개발이 아니라 **승인 항목을 1페이지로 압축하고, 승인된 기준을 API/UI/검증 기준으로 고정하는 것**이다.

### 0417 실행 순서

1. Conversion Dictionary v1 승인 항목 압축.
2. biocom, thecleancoffee, AIBIO별 source-of-truth 표 고정.
3. freshness 상태를 `실시간 추정/잠정/확정/stale`로 고정.
4. CAPI/Purchase Guard/sync 검증 evidence 정리.
5. 이후 `Revenue Integrity Agent`의 `/api/integrity/*` 계약으로 연결.

---

## 스프린트 현황 요약

| 스프린트 | 완성도 | 현재 상태 | 남은 작업 | 완료 기준 |
|---|---:|---|---|---|
| Sprint 0. Conversion Dictionary / 정본 기준선 | 70% | `roadmap/phase0_codex_draft.md`와 `roadmap/phase0.md`에 Conversion Dictionary, freshness, backend contract 초안 작성 완료 | TJ님 승인, source-of-truth 표 확정, 승인 기준을 `/api/integrity/*`와 운영 화면 언어로 반영 | 어떤 숫자가 왜 다른지 말하기 전에 "비교 가능한 숫자인지"를 먼저 판정 가능 |
| Sprint 1. CAPI/Purchase Guard clean baseline | 72% | 바이오컴 guard v3, 더클린커피 footer/CAPI v3 설치 완료. 바이오컴 2026-04-13부터 교정된 POST 창 확인 중 | 더클린커피는 2026-04-16부터 clean baseline을 별도 축적. Meta Events Manager에서 browser/server dedup, EMQ 확인 | 사이트별 7일 닫힌 창에서 duplicate order/event group 0, CAPI 2xx 성공, Meta/Attribution 격차가 설명 가능 |
| Sprint 2. Attribution identity coverage | 55% | VM 최신 all-three coverage: 전체 payment_success 50.26%, 더클린커피 50.79%. checkout_started는 전체 93.06%, 더클린커피 93.33%로 양호 | payment_success에서 `ga_session_id`, `client_id`, `user_pseudo_id`가 빠지는 경로 보강. 결제완료 페이지와 서버 호출 모두 원장 저장 | payment_success all-three coverage 80% 이상, 주문 단위로 Meta CAPI event_id와 내부 광고 기여 주문 비교 가능 |
| Sprint 3. 캠페인 매핑/LTV ROAS | 65% | 바이오컴 `utm_id`, `utm_term` adset, manual alias fallback 반영. `/ads`, `/biocom-ltv-cac`에 Meta ROAS/Att ROAS/LTV ROAS 표시 | VM 배포 또는 로컬 ledger 동기화. 상담 DB 연락처 조인 품질 확인. 더클린커피 캠페인 매핑 seed 추가 | 캠페인별 confirmed 매출 커버리지 80% 이상, 상담·영양제 후속 전환 캠페인만 증액 후보로 자동 분류 |
| Sprint 4. 더클린커피 ROAS/퍼널 진단 | 48% | Meta 계정 연동 완료. 최근 7일 1d_click 기준 Meta ROAS 1.66x, Att ROAS 0.32x, 클릭→랜딩 78.0%, 랜딩→구매 5.77% | CAPI 스냅샷이 2026-04-15 KST라 2026-04-16 이후 clean day 필요. 결제완료 identity coverage 보강 | 2026-04-23 아침 7일 clean baseline으로 광고 효율과 Attribution 누락을 분리 판단 |
| Sprint 5. Clarity UX/Checkout 병목 | 35% | 3사이트 Clarity 설치 완료. 더클린커피는 퍼널상 랜딩/구매 전환 자체는 강한 편 | Clarity 세션 리플레이, Dead click, Rage click, 모바일 결제 이탈 수동 리뷰. 장바구니/결제 단계별 이벤트 연결 | UX 병목 후보가 URL/디바이스/버튼 단위로 3개 이상 도출되고 개선 실험으로 연결 |
| Sprint 6. Incrementality OS loop | 25% | iROAS 엔진은 Phase 5.5-S3까지 구현 완료. 실험 설계 필요 | 7일 clean baseline 후 holdout 대상, 비율, 기간, 중단 조건 정의. 캠페인 매핑 보정 전 live 실험은 보류 | 광고를 클릭한 매출이 아니라 광고 때문에 생긴 증분매출을 iROAS로 주간 리포트 |

### Sprint 1. CAPI/Purchase Guard clean baseline

무엇:

브라우저 Pixel, 서버 CAPI, Purchase Guard가 같은 주문을 같은 의미로 기록하게 만드는 작업이다.
쉽게 말하면, 한 주문에 대해 "아임웹 결제완료", "Meta Pixel Purchase", "서버 CAPI Purchase", "우리 Attribution 원장"이 서로 다른 말을 하지 않게 맞추는 것이다.

비유:

택배 송장과 같다. 물건은 하나인데 택배사 송장, 쇼핑몰 주문번호, 고객센터 주문번호가 서로 다르면 나중에 누가 샀는지, 배송됐는지, 반품됐는지 맞출 수 없다. 지금의 `event_id`, `order_code`, `order_no`, `paymentKey`가 바로 송장번호 역할을 한다. 이 번호들이 같은 주문을 같은 주문으로 가리켜야 Meta ROAS와 내부 ROAS를 비교할 수 있다.

왜:

가상계좌 미입금 주문이 Purchase로 잡히거나, 같은 주문이 서로 다른 `event_id`로 여러 번 서버 전송되거나, biocom 주문이 coffee source로 섞이면 Meta는 매출이 있었다고 보는데 내부 원장은 없다고 볼 수 있다. 그러면 "광고가 좋은가"가 아니라 "데이터가 서로 다른 주문을 보고 있는가"를 비교하게 된다.

어떻게:

1. VM CAPI 로그에서 최근 운영 결제만 본다.
   - `scope=recent_operational`
   - Meta 2xx 성공률
   - duplicate event_id
   - duplicate order/event group
   - 같은 주문+이벤트에 event_id가 여러 개인지 확인

2. Meta Events Manager에서 브라우저 Pixel과 서버 CAPI가 dedup되는지 본다.
   - 같은 `event_id`로 browser/server가 들어오는지
   - Event Match Quality가 너무 낮지 않은지
   - Purchase와 VirtualAccountIssued가 의도대로 분리되는지

3. clean baseline 날짜를 분리한다.
   - 바이오컴: 2026-04-13부터 교정된 POST 창으로 본다.
   - 더클린커피: 2026-04-15 CAPI/footer v3 배포, 2026-04-15 20:53 KST source-label 교정 이후부터 안정화 구간으로 본다.
   - 더클린커피의 운영 판단 기준일은 2026-04-16 이후 닫힌 날짜다.

Codex가 할 일:

- VM CAPI 로그를 조회해 `success/failure`, duplicate event/order group, 위험 주문 샘플을 정리한다.
- `/ads`에 clean baseline 기준일과 현재 해석을 표시한다.
- 위험 이벤트가 나오면 주문번호, event_id, pixel_id, source, send_path를 추적한다.

TJ님이 할 일:

- Meta Events Manager 화면에서 browser/server dedup 상태와 Event Match Quality를 확인한다.
- 운영 중인 광고비를 갑자기 크게 바꾸지 않고, clean baseline이 쌓일 때까지 유지한다.
- Events Manager에 경고 문구가 있으면 캡처를 공유한다.

### Sprint 2. Attribution identity coverage

무엇:

결제 시작과 결제완료가 같은 사용자/세션/주문으로 이어지도록 `ga_session_id`, `client_id`, `user_pseudo_id`를 자체 원장에 저장하는 작업이다.

비유:

매장에 손님이 들어올 때 입구에서 번호표를 줬는데, 계산대에서 번호표를 회수하지 못하면 "이 계산이 어느 손님이 한 것인지" 모른다. `checkout_started`는 입구 번호표를 받은 상태이고, `payment_success`는 계산대다. 지금 문제는 입구에서는 번호표가 잘 찍히는데, 계산대에서 번호표가 절반 정도 사라진다는 것이다.

현재 수치:

| 구분 | payment_success all-three | checkout_started all-three | 해석 |
|---|---:|---:|---|
| 전체 VM 최신 | 50.26% | 93.06% | 결제 시작은 식별자가 잘 붙고, 결제완료에서 많이 떨어진다 |
| 더클린커피 | 50.79% | 93.33% | 더클린커피도 같은 패턴이다 |

여기서 all-three는 `ga_session_id`, `client_id`, `user_pseudo_id`가 모두 있는 비율이다.
목표는 payment_success all-three 80% 이상이다.

왜:

Meta ROAS와 내부 Att ROAS 차이를 줄이려면 "광고 클릭 → 랜딩 → 결제 시작 → 결제완료 → CAPI 전송"이 한 줄로 이어져야 한다. payment_success에서 식별자가 빠지면 주문은 존재하지만, 그 주문이 어떤 광고 클릭에서 왔는지 확정하기 어렵다. 그러면 Att ROAS가 실제보다 낮게 보일 수 있다.

어떻게:

1. 결제 시작 시점에 식별자를 먼저 저장한다.
   - `ga_session_id`
   - `client_id`
   - `user_pseudo_id`
   - `fbclid`, `_fbp`, `_fbc`
   - UTM
   - 임시 checkout/session id

2. 결제완료 시점에 주문 키와 식별자를 다시 묶는다.
   - `order_code`
   - `order_no`
   - `payment_code`
   - Toss `paymentKey`
   - Toss `orderId`

3. 결제완료 서버 호출이 들어오면 기존 checkout row와 조인한다.
   - 같은 브라우저 저장소 값이 있으면 직접 저장
   - 브라우저 값이 비어 있으면 `order_no/order_code/paymentKey/orderId`로 checkout_started row에서 carry-over
   - 최종적으로 payment_success row의 `ga_session_id/client_id/user_pseudo_id`를 채운다

4. coverage를 매일 확인한다.
   - 전체 payment_success all-three
   - 더클린커피 payment_success all-three
   - checkout_started 대비 payment_success 하락 폭
   - 최근 누락 샘플 URL과 missingFields

Codex가 할 일:

- 원장 저장 로직을 보강한다.
- 결제완료 페이지 스크립트와 서버 수신 API에서 `ga_session_id/client_id/user_pseudo_id`를 빠짐없이 받도록 수정한다.
- `checkout_started -> payment_success` carry-over 로직을 만든다.
- `/ads`에서 전체/사이트별 coverage를 계속 표시한다.

TJ님이 할 일:

- 아임웹 관리자에서 footer/header/custom code 교체가 필요한 경우 배포를 승인하고 반영한다.
- 테스트 주문 또는 실제 소액 주문으로 결제 시작부터 결제완료까지 한 번 흐르게 한다.
- 아임웹/토스 관리자 화면에서 접근 권한, 결제완료 페이지 커스텀 가능 여부, 웹훅/리다이렉트 설정 가능 여부를 확인한다.

### Sprint 2-1. 아임웹/토스/내부 개발 가능 범위 판단

결론:

아임웹은 외부 솔루션이므로 아임웹 내부 결제 엔진 자체를 우리가 수정할 수는 없다.
하지만 지금처럼 아임웹 사이트의 header/footer/custom code, GTM, 외부 서버 API, 아임웹/토스 조회 API를 조합해서 "겉에서 감싸는 방식"의 개발은 가능하다. 이 방식은 지금까지 footer CAPI, Purchase Guard, attribution snippet에서 이미 사용한 방식이다.

즉 "아임웹 코어를 고친다"가 아니라 "아임웹 페이지 위에 추적 레이어를 얹고, 우리 서버 원장과 연결한다"가 현실적인 방식이다.

비유:

아임웹은 임대 매장이다. 벽을 허물거나 계산대 내부 프로그램을 뜯어고칠 수는 없다. 대신 매장 입구에 방문 기록 카메라를 달고, 계산대 앞에 영수증 스캐너를 두고, 우리 사무실 서버로 기록을 보내는 것은 가능하다. 지금 우리가 하는 footer 코드와 서버 원장이 그 카메라와 스캐너 역할이다.

우리가 직접 개발 가능한 것:

| 영역 | 가능 여부 | 설명 |
|---|---|---|
| 아임웹 header/footer/custom code 삽입 | 가능 | 지금처럼 footer 코드로 UTM, GA 식별자, fbq event_id, CAPI mirror를 넣을 수 있다 |
| GTM/Clarity/Meta Pixel 보강 | 가능 | 태그와 브라우저 이벤트 수집은 우리가 제어 가능하다 |
| 자체 서버 원장 API | 가능 | `att.ainativeos.net` 또는 로컬 백엔드가 payment_success/checkout_started를 받을 수 있다 |
| checkout_started와 payment_success carry-over | 가능 | 브라우저 저장소 + 서버 원장 + 주문 키 조인으로 구현 가능하다 |
| Toss API 조회/검증 | 가능 | `paymentKey`, `orderId`, `DONE` 상태 조회로 confirmed 결제를 검증할 수 있다 |
| 아임웹 주문 API 조회/검증 | 가능 | 현재 레거시 v2 주문 API는 사용 중이고, Open API는 권한 확인이 더 필요하다 |

우리가 직접 고칠 수 없는 것:

| 영역 | 불가능/제한 | 설명 |
|---|---|---|
| 아임웹 결제 엔진 내부 로직 | 제한 | 아임웹 SaaS 내부 코드라 직접 수정 불가 |
| 결제완료 페이지가 렌더링되기 전 서버 내부 흐름 | 제한 | 아임웹이 처리하는 서버 사이드 흐름은 외부에서 직접 패치 불가 |
| Toss 결제창 내부 동작 | 제한 | Toss는 결제 승인/리다이렉트/웹훅을 제공하는 PG이지, GA 식별자를 보관하는 CRM이 아니다 |
| Toss에 GA 식별자 저장 | 비권장 | 개인/광고 식별자는 우리 first-party 원장에 저장하는 것이 맞다 |

아임웹에 확인할 것:

- 결제완료 페이지에서 custom script가 실행되는 정확한 시점
- `order_code`, `order_no`, `payment_code`를 프론트에서 안정적으로 읽을 수 있는지
- 결제완료 페이지/주문완료 페이지별 script 삽입 가능 범위
- 결제완료 서버 호출 또는 외부 연동 webhook에 커스텀 파라미터를 실을 수 있는지
- Open API 주문 조회 권한/OAuth 앱 발급 가능 여부

토스페이먼츠에 확인할 것:

- `successUrl` 리다이렉트에서 `paymentKey`, `orderId`, `amount`가 어떤 조건으로 붙는지
- 웹훅에서 결제 상태 `DONE` 또는 가상계좌 입금 상태를 안정적으로 받을 수 있는지
- `orderId`가 아임웹 `order_no-P1` 형태로 계속 유지되는지
- 결제 승인 이후 조회 API로 confirmed/DONE 상태를 어느 시점부터 확정할 수 있는지

공식 문서 기준 참고:

- Toss Payments 개발자 문서 기준, 결제 인증 성공 후 `successUrl`로 리다이렉트되고 query parameter로 다음 승인 단계에 필요한 값이 붙는다. 참고: https://docs.tosspayments.com/blog/what-is-successurl
- Toss Payments는 결제 조회/승인/웹훅으로 결제 상태를 다루는 역할이다. 참고: https://docs.tosspayments.com/en/api-guide
- Imweb 개발자 문서는 Site, Order, Product, Member 등 API 범주를 제공한다. 다만 현재 우리 레거시 v2 토큰으로 Open API 주문 권한이 모두 열려 있지는 않아 권한 확인이 필요하다. 참고: https://developers-docs.imweb.me/

Codex가 개발 가능한가:

가능하다. 단, 범위는 "아임웹 내부 코어 수정"이 아니라 아래 범위다.

1. footer/header/GTM에 붙일 외부 코드 작성
2. checkout_started/payment_success caller 수정
3. 자체 원장 API와 DB 저장 로직 수정
4. CAPI event_id/dedup 로직 수정
5. coverage 리포트와 `/ads` UI 개선
6. Toss/Imweb API를 이용한 주문 상태 검증 및 조인 로직 개발

TJ님이 해야 하는 것은 운영 권한이 필요한 부분이다.

1. 아임웹 관리자에 코드 붙여넣기
2. 아임웹/토스 고객센터 또는 담당자에게 권한/설정 가능 여부 문의
3. Meta Events Manager 화면 확인
4. 테스트 주문 승인
5. 실제 운영 배포 타이밍 결정

### Sprint 3. 캠페인 매핑/LTV ROAS

무엇:

캠페인별로 Meta ROAS, 내부 confirmed Att ROAS, 180일 후속 구매를 포함한 LTV ROAS를 나란히 보는 작업이다.

비유:

전체 매출만 보는 것은 "학교 평균 점수"만 보는 것과 같다. 평균은 괜찮아도 어떤 학생이 잘하고 어떤 학생이 못하는지 모르면 보충수업을 누구에게 해야 할지 모른다. 캠페인 매핑은 각 학생 이름표를 붙이는 작업이다.

왜:

전체 광고 ROAS가 좋아도 어떤 캠페인이 진짜 confirmed 매출을 만드는지 모르면 예산을 어디에 늘리고 어디에서 줄일지 결정할 수 없다. 특히 바이오컴은 상담·영양제 후속 전환이 붙는 캠페인이 장기적으로 더 중요하다.

어떻게:

1. `utm_id`를 Meta campaign id 1순위로 본다.
2. `utm_term`을 adset id로 보고 부모 campaign을 찾는다.
3. 이름만 비슷한 `utm_campaign`은 fallback으로만 쓴다.
4. `fbclid` only는 Meta 유입 가능성은 높지만 캠페인 확정으로 강제 배분하지 않는다.
5. 확정된 campaign id에 confirmed 주문을 붙이고, 고객번호/전화번호/order key로 상담·영양제 후속 구매를 조인한다.

Codex가 할 일:

- 캠페인 매핑 로직을 VM 최신 원장에 맞게 배포하거나 동기화한다.
- 상담 DB와 주문 DB 조인 키 품질을 확인한다.
- `/ads`, `/biocom-ltv-cac`에 캠페인별 Meta ROAS/Att ROAS/LTV ROAS를 계속 개선한다.

TJ님이 할 일:

- Meta 광고관리자에서 campaign/adset/ad 이름과 URL 태그가 실제 의도와 맞는지 확인한다.
- "유지해야 하는 전략 캠페인"과 "감액 가능한 캠페인"을 구분해 준다.
- 상담 완료/영양제 구매 기준이 비즈니스적으로 어떤 상태인지 정의한다.

### Sprint 4. 더클린커피 ROAS/퍼널 진단

무엇:

더클린커피의 낮은 Att ROAS가 실제 광고 부진인지, 아니면 CAPI/Attribution 전환 직후라 내부 귀속이 덜 잡힌 것인지 분리한다.

비유:

체중계를 새로 바꾼 첫날의 숫자만 보고 살이 빠졌다고 판단하면 안 된다. 이전 체중계와 새 체중계의 기준이 다를 수 있다. 지금 더클린커피도 2026-04-15에 측정 장비가 바뀌었으므로, 최소 며칠은 새 기준으로 닫힌 데이터를 쌓아야 한다.

왜:

최근 7일 1d_click 기준 Meta 구매는 25건/₩933,625인데 내부 Attribution confirmed는 5건/₩177,630이다. 반면 사이트 전체 confirmed는 55건/₩2,672,529다. 광고가 전부 나쁜 것이 아니라, 광고 귀속 주문이 내부에서 덜 붙고 있을 가능성이 크다.

어떻게:

1. 2026-04-15 KST CAPI/footer v3 배포일은 전환일로 본다.
2. 2026-04-16부터 닫힌 날짜만 clean baseline으로 본다.
3. 2026-04-17 아침에는 1일 신호, 2026-04-19 아침에는 3일 신호, 2026-04-23 아침에는 7일 baseline으로 본다.
4. Meta ROAS, Att ROAS, 사이트 전체 confirmed 상한, payment_success coverage를 같이 본다.

Codex가 할 일:

- `/ads` 더클린커피 탭에 clean baseline 기준일과 낮은 Att ROAS 원인을 계속 표시한다.
- 날짜가 쌓이면 자동으로 1일/3일/7일 판정을 업데이트한다.
- CAPI 로그와 Attribution ledger를 대조해 광고 부진인지 귀속 누락인지 분해한다.

TJ님이 할 일:

- 2026-04-23 전까지 더클린커피 광고 예산을 큰 폭으로 줄이지 않는다.
- Meta Events Manager에서 더클린커피 pixel 수신/중복/품질 경고를 확인한다.
- 더클린커피 실제 주문/취소/가상계좌 입금 흐름에서 특이 케이스가 있으면 공유한다.

### Sprint 5. Clarity UX/Checkout 병목

무엇:

랜딩 이후 실제 사용자가 어디서 좌절하거나 이탈하는지 Clarity로 확인한다.

비유:

ROAS 숫자는 매출 영수증이고, Clarity는 매장 CCTV다. 영수증만 보면 누가 왜 안 샀는지 모른다. CCTV를 보면 손님이 가격표 앞에서 멈췄는지, 결제 버튼을 여러 번 눌렀는지, 장바구니에서 나갔는지 볼 수 있다.

왜:

더클린커피의 클릭→랜딩 78.0%, 랜딩→구매 5.77%는 나쁜 퍼널이 아니다. 그래서 지금은 페이지 전체를 뜯어고치는 것보다 결제 단계, 복귀 단계, 식별자 유실 구간을 좁혀 보는 것이 더 효율적이다.

어떻게:

1. 상품 옵션 선택 후 구매 버튼까지 막히는지 본다.
2. 장바구니/결제수단 선택에서 Dead click과 Rage click을 본다.
3. 네이버페이, 카카오싱크, 토스 리다이렉트 후 결제완료 페이지로 잘 돌아오는지 본다.
4. 모바일 세션을 우선 본다.
5. UX 이슈와 tracking 이슈를 분리한다. 사용자는 잘 샀는데 tracking만 끊긴 경우가 있기 때문이다.

Codex가 할 일:

- `/ads`에 Clarity에서 볼 항목과 현재 퍼널 해석을 정리한다.
- 가능한 경우 Clarity API 또는 export를 받아 지표화하는 개발을 검토한다.
- UX 병목과 Attribution 병목을 분리해서 리포트한다.

TJ님이 할 일:

- Clarity 대시보드에서 세션 리플레이를 직접 확인한다.
- Dead click/Rage click이 많은 페이지나 버튼을 캡처한다.
- 실제 고객 문의나 결제 불편 사례가 있으면 해당 URL/시간대와 함께 공유한다.

### Sprint 6. Incrementality OS loop

무엇:

ROAS가 아니라 iROAS, 즉 광고 때문에 추가로 생긴 매출을 측정하는 루프를 만든다.

비유:

우산을 쓴 날 바닥이 젖어 있었다고 해서 우산 때문에 비가 온 것은 아니다. Meta ROAS도 비슷하다. 광고를 본 사람이 샀다는 사실과, 광고 때문에 샀다는 사실은 다르다. iROAS는 "광고를 끄면 매출이 얼마나 줄어드는가"를 직접 보는 방식이다.

왜:

ROAS 3x도 원래 샀을 고객이면 증분 가치가 낮다. 반대로 ROAS 2x라도 전부 신규 고객이면 매우 가치 있는 광고비일 수 있다. 장기적으로 광고비 증액 판단은 ROAS보다 iROAS가 더 강한 근거다.

어떻게:

1. 7일 clean baseline을 먼저 확보한다.
2. 캠페인 매핑과 payment_success coverage를 보정한다.
3. holdout 대상과 비율을 정한다.
   - 캠페인 holdout
   - 지역/세그먼트 holdout
   - CRM 메시지 holdout
4. 실험 기간, 성공 지표, 중단 조건을 정한다.
5. 실험 후 광고비를 유지/증액/감액한다.

Codex가 할 일:

- 실험 설계안을 문서화한다.
- iROAS 계산 엔진과 대시보드를 연결한다.
- holdout 전후 매출, 신규 고객, 재구매, 상담·영양제 후속 전환을 계산한다.

TJ님이 할 일:

- holdout을 허용할 수 있는 비율을 정한다. 예: 5%, 10%, 15%.
- 절대 끄면 안 되는 캠페인과 실험 가능한 캠페인을 구분한다.
- 실험 중 매출 감소를 어느 정도까지 허용할지 결정한다.

---

## 현재 결론

전체 광고비는 **유지**한다. 무차별 증액은 아직 하지 않는다.

현재 바이오컴 3일 기준:

| 항목 | 값 |
|---|---:|
| 광고비 | ₩12,361,899 |
| Attribution confirmed 매출 | ₩23,428,300 |
| Attribution ROAS | **1.90x** |
| Meta Purchase value | ₩37,841,869 |
| Meta ROAS | **3.06x** |
| Meta / Attribution 격차 | **1.61배** |

4/14~15만 보면 격차는 약 1.34배까지 내려왔다. 지금은 CAPI/Purchase Guard 실패보다 **Meta의 넓은 매칭 + 내부 캠페인 매핑 미완성**이 더 큰 원인으로 보인다.

비즈니스 판단:

- Attribution ROAS 1.90x는 기여이익률 약 53%가 1차 구매 손익분기다.
- 실제 기여이익률이 50% 미만이면 현재 광고는 1차 구매만으로 공격적 증액하기 어렵다.
- 재구매/LTV 또는 신규 고객 비중이 높다면 현재 광고비는 성장에 도움이 된다.
- 그래서 다음 액션은 총액 증액이 아니라 **동일 예산 안에서 정합성 보정과 캠페인별 재배분**이다.

---

## 가장 중요한 다음 스텝

2026-04-17 기준 실행 순서는 아래로 보정한다.

1. **Conversion Dictionary v1 승인 패키지**
   - `confirmed`, `paid`, `pending`, `canceled`, `refunded`, `VirtualAccountIssued` 정의를 운영 승인 가능한 1페이지로 압축한다.
   - 특히 `confirmed = Imweb PURCHASE_CONFIRMATION`으로 둘지, Toss PG 승인 매출까지 포함할지 TJ님 승인이 필요하다.
   - 승인 전에는 Revenue Integrity Agent와 iROAS live 판단을 확장하지 않는다.

2. **Freshness/source-of-truth 고정**
   - biocom, thecleancoffee, AIBIO별 primary/secondary source를 표로 잠근다.
   - GA4/Meta/Imweb/Toss/Attribution Ledger/CAPI log/CRM DB를 `실시간 추정/잠정/확정/stale`로 분류한다.
   - stale와 pending은 성과 문제로 보지 않고 데이터 신뢰도 문제로 분리한다.

3. **CAPI/Purchase Guard/sync evidence 정리**
   - 2026-04-13 이후 바이오컴 clean window와 2026-04-16 이후 커피 clean window를 분리한다.
   - VM `/health`, CAPI log, Events Manager dedup/EMQ, 실주문 pending->confirmed 증거를 한 묶음으로 남긴다.

4. **캠페인 매핑 보정**
   - 아래 기존 섹션의 작업은 계속 중요하다.
   - 다만 캠페인별 증액/감액 판단은 정본/freshness와 7일 clean baseline 이후로 둔다.

5. **얇은 loop 운영 방식**
   - 복잡한 작업에만 `plans/*.md`를 쓰고, 완료 보고마다 evidence와 재검증 task를 남긴다.
   - `.codex/agents`, `.claude/agents`, 자동 PR, production write-back은 아직 하지 않는다.

### 1. 캠페인 매핑 보정

현재 2026-04-13~15 내부 Attribution 캠페인 매출이 대부분 `(unmapped)`로 몰려 있다.
이 상태에서는 사이트 전체 ROAS는 읽을 수 있지만, 어떤 캠페인을 증액/감액할지 내부 confirmed 기준으로는 확정할 수 없다.

임팩트:

- 광고비 증액/감액 판단의 정확도를 가장 크게 올린다.
- Meta ROAS가 높은 캠페인이 실제 confirmed 매출도 만드는지 확인할 수 있다.
- 예산 재배분이 가능해진다.

### 1-1. 내부 Attribution 캠페인 분류 검토

검토 기준: VM `https://att.ainativeos.net`, `biocom_imweb` Attribution ledger 최신 200개, 그중 `payment_success + confirmed` 45건 / ₩12,910,763 샘플.
Meta 캠페인 계층은 `/api/meta/campaigns/:campaignId/detail`에서 광고세트 ID까지 대조했다.

분류 원칙:

- `utm_id`가 Meta campaign id와 일치하면 **확정**한다.
- `utm_term`이 Meta adset id이고, 캠페인 상세에서 부모 campaign이 확인되면 **확정**한다.
- `utm_campaign`/`utm_source` 이름만 있고 campaign id/adset id가 없으면 **보류**한다.
- `fbclid`만 있으면 Meta 유입 가능성은 높지만 내부 데이터만으로 campaign을 특정하지 않는다.
- `naver`, `topbanner`, `inpork`, direct/blank 중 `fbclid`가 없는 주문은 Meta 캠페인 분류에서 제외한다.

확정 가능한 매핑:

| 내부 Attribution 단서                                                                                                                            | 확정 Meta 캠페인                                              | 근거                                                          |     샘플 confirmed |
| -------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ----------------------------------------------------------- | ---------------: |
| `meta_biocom_songyuul08`, `utm_term=120242626179270396`, 일부 `utm_id=120242626179290396`                                                      | `120242626179290396` 공동구매 인플루언서 파트너 광고 모음_3 (260323)     | `utm_id` 직접 일치 + adset `120242626179270396`의 부모 campaign 확인 | 13건 / ₩4,015,700 |
| `meta_biocom_yeonddle_igg`, `meta_biocom_igevsiggblog_igg`, `meta_biocom_iggpost_igg`, `utm_term=120229949925450396` 또는 `120213362391830396` | `120213362391690396` [바이오컴] 음식물 과민증 검사 전환캠페인(10/14~)     | `utm_id` 직접 일치 또는 adset 부모 campaign 확인                      |  4건 / ₩1,209,000 |
| `meta_biocom_haengza01_acid`, `utm_term=120237452088100396`                                                                                  | `120237452088280396` [바이오컴] 종합대사기능검사 전환캠페인(11/4~) - 사본   | adset `120237452088100396`의 부모 campaign 확인                  |    2건 / ₩536,400 |
| `meta_biocom_biospeed_mineral`, `utm_id=120218496689750396`, `utm_term=120218496689740396`                                                   | `120218496689750396` [바이오컴] 영양중금속검사 전환 캠페인               | `utm_id` 직접 일치 + adset 부모 campaign 확인                       |    1건 / ₩268,200 |
| `meta_biocom_sikdanstory_igg`, `utm_term=120235591897230396`                                                                                 | `120235591897270396` [바이오컴] 음식물 과민증 검사 어드밴티지+캠페인(251020) | adset `120235591897230396`의 부모 campaign 확인                  |    1건 / ₩245,000 |

즉, 최신 샘플 기준으로 Meta 캠페인까지 확정 가능한 주문은 **21건 / ₩6,274,300**이다.
현재 `/api/ads/roas`는 `utm_campaign`만 캠페인명/id에 매칭하기 때문에 위 확정 가능 매출도 `(unmapped)`로 빠진다. 이건 데이터 부재가 아니라 **매핑 로직 미흡**이다.

보류로 남길 항목:

| 내부 Attribution 단서 | 샘플 confirmed | 보류 이유 |
|---|---:|---|
| blank UTM + `fbclid`만 존재 | 3건 / ₩961,200 | Meta 유입은 맞을 가능성이 높지만 campaign/adset id가 없어 내부 데이터만으로 캠페인 특정 불가 |
| `meta_biocom_songyuul08`지만 `utm_term`, `utm_id`, `fbclid` 없음 | 1건 / ₩459,000 | 랜딩 경로상 같은 공동구매 캠페인일 가능성은 높지만, 확정 단서가 없음 |
| `meta_biocom_skincare_igg`지만 `utm_term`, `utm_id`, `fbclid` 없음 | 1건 / ₩245,000 | Meta 명명 규칙은 보이나 campaign/adset id가 없어 보류 |

Meta 캠페인 분류에서 제외할 항목:

| 내부 Attribution 단서 | 샘플 confirmed | 판단 |
|---|---:|---|
| direct/blank + `fbclid` 없음 | 14건 / ₩3,805,263 | Meta 캠페인 근거 없음 |
| `naver`, `naverbrandsearch_*`, `NaPm` 계열 | 3건 / ₩698,000 | 네이버 유입으로 분리 |
| `topbanner_mo` | 1건 / ₩234,000 | 사이트 내부 배너로 분리 |
| `inpork_biocom_acid` | 1건 / ₩234,000 | 파트너/인플루언서성 유입으로 보되 Meta campaign에는 직접 매핑하지 않음 |

Codex 구현 방향:

1. `utm_id`를 campaign id 1순위로 매칭한다.
2. `utm_term`을 adset id로 보고 Meta campaign detail에서 부모 campaign을 확인해 매칭한다.
3. `utm_campaign` 이름 매칭은 3순위 fallback으로만 사용한다.
4. `fbclid` only 주문은 campaign으로 강제 배분하지 않고 `Meta unattributed campaign` 보류 버킷으로 남긴다.
5. `/ads`에는 `확정: utm_id`, `확정: adset`, `보류: fbclid only`, `제외: non-Meta` 신뢰도 배지를 붙인다.

상담·영양제 전환 캠페인만 발라내는 방법:

1. **캠페인 확정 게이트**
   - `utm_id`가 campaign id와 일치하거나, `utm_term` adset id의 부모 campaign이 확인된 주문만 후보로 둔다.
   - `fbclid` only, `meta_*` source only, 내부 배너/네이버/파트너 유입은 증액 후보에서 제외한다.

2. **결제 확정 게이트**
   - `payment_success + confirmed` 또는 Toss confirmed 주문만 본다.
   - 가상계좌 `pending`, 테스트 이벤트, CAPI retry/duplicate 의심 이벤트는 예산 판단에서 제외한다.

3. **상담·영양제 후속 게이트**
   - 주문의 고객 식별자(`order_member`, phone hash, customer key, orderId)를 상담 원장과 연결한다.
   - 연결된 고객이 `상담 완료` 상태이거나, 90/180일 안에 `productCategory=supplement` 주문을 만든 경우만 “상담·영양제 전환 확인”으로 판정한다.
   - 1차 구매 상품이 검사여도 후속 영양제 구매가 붙으면 증액 후보가 될 수 있다.

4. **증액 테스트 게이트**
   - clean window에서 캠페인별 confirmed 주문 2건 이상 또는 confirmed 매출 ₩500,000 이상을 최소 조건으로 둔다.
   - 이 조건을 넘고 상담·영양제 후속 전환이 붙은 캠페인만 +10~15% 소액 증액한다.
   - 증액 후 48~72시간은 전체 예산을 유지하고, campaign-level Attribution ROAS와 상담·영양제 후속 전환만 본다.

현재 1차 확인 후보:

| 캠페인 | 현재 확인된 것 | 바로 증액 여부 |
|---|---|---|
| `120242626179290396` 공동구매 인플루언서 파트너 광고 모음_3 | campaign-level confirmed 매출은 가장 큼 | 상담·영양제 후속 join 확인 전까지 보류 |
| `120213362391690396` 음식물 과민증 검사 전환캠페인 | campaign/adset 매핑 확정, 검사 → 영양제 후속 가능성이 큰 상품군 | 상담·영양제 후속 join 우선 확인 |
| `120237452088280396` 종합대사기능검사 전환캠페인 | campaign/adset 매핑 확정 | 상담·영양제 후속 join 확인 후 소액 테스트 |
| `120218496689750396` 영양중금속검사 전환 캠페인 | `utm_id` 직접 확인 | 표본 1건이라 추가 confirmed 필요 |
| `120235591897270396` 음식물 과민증 검사 어드밴티지+캠페인 | adset 매핑 확정 | 표본 1건이라 추가 confirmed 필요 |

따라서 지금 당장 증액할 캠페인을 감으로 고르지 않는다.
Codex가 먼저 `campaign_id -> confirmed order -> consultation completed/supplement order` 조인 리포트를 만들고, 그 리포트에서 통과한 캠페인만 소액 증액 테스트한다.

2026-04-16 구현 현황:

- `/api/ads/roas`에 `utm_id` campaign id, `utm_term` adset id, local alias audit fallback, `manual_verified` alias 매핑을 반영했다.
- `/api/ads/campaign-ltv-roas`를 추가했다. 계산식은 `campaign -> confirmed order -> customer_number/phone -> 180일 후속 주문` 조인이다.
- Meta Graph adset 조회가 rate limit이면 `data/meta_campaign_alias_audit.biocom.json`의 adset 계층을 fallback으로 사용한다.
- `/ads` 캠페인별 성과 표에 `Meta ROAS`, `Att ROAS`, `LTV 기준 ROAS`를 동시에 표시했다.
- `/biocom-ltv-cac`에 상담·영양제 전환 캠페인 선별 게이트와 캠페인별 LTV ROAS 표를 추가했다.

로컬 API 검증값: `last_7d`, 2026-04-09~15, `localhost:7020`, VM 배포 전 기준

| 캠페인 | Att 매출 | Att ROAS | LTV 매출 | LTV ROAS | 상담 고객 | 영양제 고객 | 판단 |
|---|---:|---:|---:|---:|---:|---:|---|
| 공동구매 인플루언서 파트너 광고 모음_3 | ₩6,110,000 | 1.17x | ₩6,110,000 | 1.17x | 0 | 0 | 계산 가능, 단 후속 전환 없음 |
| 음식물 과민증 검사 어드밴티지+캠페인 | ₩1,715,000 | 0.20x | ₩1,715,000 | 0.20x | 0 | 0 | 계산 가능, 증액 근거 약함 |
| 음식물 과민증 검사 전환캠페인 | ₩1,305,200 | 0.24x | ₩1,305,200 | 0.24x | 0 | 0 | 계산 가능, 증액 근거 약함 |
| 종합대사기능검사 전환캠페인 | ₩942,400 | 0.29x | ₩942,400 | 0.29x | 0 | 0 | 계산 가능, 증액 근거 약함 |
| 건강기능식품 - 클린, 풍성, 바이오 | ₩35,000 | 0.02x | ₩35,000 | 0.02x | 0 | 1 | 표본 부족 |
| 호르몬 검사 바이럴 소재 캠페인 | ₩245,000 | 0.24x | ₩245,000 | 0.24x | 0 | 0 | 표본 부족 |

중요 해석:

- 현재 로컬 LTV ROAS는 대부분 Att ROAS와 같다. 2026-04-09~15 유입 고객의 180일 후속 구매가 아직 성숙하지 않았기 때문이다.
- 상담 완료 조인이 0명으로 나온다. 이건 “상담이 없었다”가 아니라, 현재 조인 키가 `customer_number/phone` 중심이라 상담 DB의 연락처 품질과 주문 연락처가 더 검증되어야 한다는 뜻이다.
- VM 최신 site-level confirmed 매출은 2026-04-09~15 기준 ₩37,602,100인데, 로컬 캠페인 매핑/LTV API의 attributed 매출은 ₩10,352,600이다. 그래서 캠페인별 LTV 표는 지금 **후보 선별용**이고, 전체 예산 판단은 VM 일별 ROAS를 우선한다.
- 정확한 최신 캠페인별 LTV ROAS를 위해서는 이 백엔드 변경을 VM에도 배포하거나, 로컬 attribution ledger를 VM과 같은 최신 상태로 동기화해야 한다.

현재 결론:

- 지금 당장 소액 증액을 승인할 캠페인은 없다.
- 공동구매 캠페인은 Att/LTV ROAS가 가장 높지만 1.17x라서 증액보다 유지/소재 검증 대상이다.
- “담·영양제 전환이 확인되는 캠페인만 증액” 조건은 아직 통과한 캠페인이 없다. 건강기능식품 캠페인에 영양제 고객 1명이 있지만 표본과 매출이 너무 작다.
- 다음 실무 우선순위는 상담 DB 연락처 조인 품질 확인, VM 배포, 7일 clean baseline 재계산이다.

### 2. CAPI/Purchase dedup 최종 확인

서버 로그 기준으로 2026-04-13 이후 operational CAPI는 313/313 성공, duplicate event/order group 0이다.
다만 Meta Events Manager의 dedup/EMQ 화면에서 브라우저 Pixel과 서버 CAPI의 최종 처리 상태를 확인해야 한다.

임팩트:

- Meta ROAS 격차가 중복 때문인지 아닌지 확정할 수 있다.
- Phase 9 funnel CAPI를 켜기 전 안전선을 만든다.

### 3. 7일 clean baseline 확보

2026-04-13~19 닫힌 7일을 2026-04-20 아침에 다시 본다.
3일치는 방향성 판단에는 충분하지만, 예산 총량 변경이나 holdout 실험 시작에는 짧다.

임팩트:

- "유지/증액/감액" 판단을 일시적 변동이 아니라 1주 기준으로 할 수 있다.
- Incrementality 실험의 baseline이 된다.

### 4. Incrementality는 설계부터, live는 보류

지금 바로 holdout live를 시작하지 않는다.
먼저 실험 설계, 제외 조건, holdout 비율, 성공 지표를 정하고 7일 clean baseline 이후 실행한다.

임팩트:

- Meta ROAS 3.06x가 "광고 덕분의 매출"인지, 원래 샀을 고객의 매출인지 분리할 수 있다.
- 장기적으로는 광고비 증액 여부의 가장 강한 근거가 된다.

---

## Codex가 할 일

0. **Conversion Dictionary v1 승인 패키지**
   - 완료: `roadmap/phase0_codex_draft.md`에 Conversion Dictionary, 상태 매핑표, freshness, backend contract 초안을 작성했다.
   - 남은 일: TJ님 승인용 1페이지로 압축한다.
   - 남은 일: 승인된 `confirmed/paid/refunded/VirtualAccountIssued` 기준을 `/api/integrity/*`, `/ads`, `/tracking-integrity`에 반영할 수 있게 계약을 고정한다.

1. **캠페인 매핑 보정**
   - 완료: `utm_id`, `utm_term`(Meta adset id), Meta campaign/adset 계층, `manual_verified` alias를 대조하도록 백엔드를 보정했다.
   - 완료: `/ads`에 "캠페인별 Meta ROAS / Att ROAS / LTV 기준 ROAS"를 추가했다.
   - 남은 일: 같은 변경을 VM에 배포하거나 로컬 ledger를 VM 최신 상태로 동기화한다.
   - 남은 일: 상담 DB 연락처 조인 품질을 확인해 `consultedCustomers=0` 원인을 분리한다.

2. **CAPI event_id 감사 자동화**
   - 2026-04-13 이후 clean window 기준으로 duplicate event/order group 0 상태를 매일 확인한다.
   - `same_event_id_retry_like`와 `multiple_event_ids_duplicate_risk`를 분리해 경고한다.
   - 위험 이벤트가 있으면 주문번호, event_id, pixel_id, send_path를 바로 보여준다.

3. **7일 clean baseline 리포트 준비**
   - 2026-04-20 아침 기준으로 2026-04-13~19 Attribution ROAS / Meta ROAS / 격차를 리포트한다.
   - 4/13 전환일 효과를 포함/제외한 두 버전을 같이 계산한다.

4. **Incrementality 설계 초안**
   - holdout 대상: 캠페인 holdout, 지역/세그먼트 holdout, CRM 메시지 holdout 중 무엇이 현실적인지 비교한다.
   - 권장 holdout 비율, 기간, 성공 지표, 중단 조건을 문서화한다.
   - live 시작 조건을 "7일 clean baseline + 캠페인 매핑 보정"으로 둔다.

5. **/ads 운영 카드 고도화**
   - 현재 상단 의견 카드를 7일 baseline 이후 자동 업데이트되게 바꾼다.
   - 예산 판단을 `유지 / 제한 증액 / 감액 후보 / 데이터 부족` 배지로 표준화한다.

6. **얇은 loop 운영 방식 적용**
   - 2시간 이상 걸리거나 운영 검증이 필요한 작업만 `plans/*.md`에 실행 기록을 남긴다.
   - 완료 보고에는 변경점, 검증 증거, 미확인 사항, 다음 재검증 시점을 같이 남긴다.
   - 단, loop 구조 자체가 정본/freshness 작업보다 앞서지 않게 한다.

---

## TJ님이 할 일

1. **Conversion Dictionary 승인**
   - 운영 `confirmed` 기준을 승인한다. 기본안은 Imweb `PURCHASE_CONFIRMATION`, 대안은 Toss PG 승인 기준이다.
   - `paid`를 ROAS 분자에서 제외할지 승인한다.
   - 환불/부분환불, 배송비/VAT 포함 여부, CAPI Purchase 전송 기준을 승인한다.

2. **광고비 총액 유지**
   - 다음 48~72시간은 전체 예산을 크게 바꾸지 않는다.
   - 예외적으로 Meta ROAS가 높은 캠페인만 +10~15% 제한 테스트를 승인할지 결정한다.

3. **비즈니스 손익 기준 제공**
   - 바이오컴 주요 상품의 대략적인 기여이익률을 알려준다.
   - 재구매율, 평균 재구매 주기, LTV가 있으면 제공한다.
   - 이 값이 있어야 Attribution ROAS 1.90x가 "성장 투자"인지 "손익 악화"인지 판단 가능하다.

4. **Meta Events Manager 확인**
   - biocom pixel `1283400029487161`에서 Purchase dedup/EMQ 화면을 확인한다.
   - 가능하면 스크린샷 또는 화면에 보이는 문구를 공유한다.
   - 확인할 것: server event 수신, browser/server dedup 상태, EMQ 점수, event_id 경고 여부.

5. **캠페인 운영 제약 공유**
   - 절대 끄면 안 되는 브랜딩/파트너/공동구매 캠페인이 있는지 알려준다.
   - 매출만 낮아도 유지해야 하는 전략 캠페인과, 바로 감액 가능한 캠페인을 구분한다.

6. **Incrementality 실험 허용 범위 결정**
   - holdout을 허용할 수 있는 비율을 정한다. 예: 5%, 10%, 15%.
   - 광고 예산 holdout이 부담되면 CRM 메시지 holdout부터 시작한다.

---

## 오늘의 우선순위 한 줄

**캠페인을 더 쪼개기 전에, 무엇을 confirmed 매출로 볼지 먼저 잠근다.**
Conversion Dictionary v1과 freshness/source-of-truth가 고정되면, 그 기준으로 CAPI/Purchase Guard, 캠페인 매핑, Revenue Integrity Agent, iROAS를 순서대로 연결한다.
